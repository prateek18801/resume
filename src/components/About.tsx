import t from "../config/static"

const About = () => {
    return (
        <section className="mb-5">
          <h2>ABOUT</h2>
          <p>{t.about}</p>
        </section>
    );
}

export default About;
